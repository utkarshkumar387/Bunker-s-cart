import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
    headerInner: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        marginTop: '10px'
    }
}))